export type Pain = {
  date: string;
  description: string;
  userId: string;
  id: string;
};

export type PainType = {
  id: string;
  description: string;
};

export type PainTypeIntensity = PainType;
